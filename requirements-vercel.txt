# Minimal requirements for Vercel serverless functions
# Only include packages actually used in the API endpoints

# Core FastAPI and serverless
fastapi>=0.115.0
mangum>=0.17.0
pydantic>=2.0.0

# Environment and config
python-dotenv>=1.2.1

# LangChain (core only - remove unused providers)
langchain>=1.1.0
langchain-text-splitters>=0.2.0
langchain-groq>=1.1.0
langchain-classic>=1.0.0

# Database
pymongo>=4.15.4

# HTTP requests
requests>=2.31.0

# Utilities
loguru>=0.7.3
jinja2>=3.1.6
rapidfuzz>=3.14.3

# Note: Excluded large/unused packages to reduce size:
# - ollama (not used in API - ~100MB+)
# - pdfplumber (not used in API - ~50MB+, API receives text directly)
# - python-docx (not used in API - ~10MB, API receives text directly)
# - docx2txt (not used in API - ~5MB, API receives text directly)
# - langchain-google-genai (not used, only Groq - ~20MB+)
# - uvicorn (not needed, mangum handles it - ~10MB+)
# - apscheduler (not used on Vercel - ~5MB)
# - node/npm (not needed for Python - ~50MB+)
# - numpy (check if actually needed - ~20MB+)

